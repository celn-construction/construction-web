// Route placeholder — Gantt is rendered by ProjectShell (keep-alive pattern).
// This file must exist so Next.js resolves the /gantt route for sidebar active state.
export default function GanttPage() {
  return null;
}
