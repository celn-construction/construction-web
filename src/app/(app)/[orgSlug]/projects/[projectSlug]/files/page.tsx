// Route placeholder — Files is rendered by ProjectShell (keep-alive pattern).
// This file must exist so Next.js resolves the /files route for sidebar active state.
export default function FilesPage() {
  return null;
}
