# SRC Documentation

This folder contains generated references for the application source tree:

- `app-routes-reference.md`
  - Full inventory of `src/app` pages, layouts, loading UIs, and API routes
  - Includes folder scope, public route scope, render mode, and key dependencies
- `components-reference.md`
  - Full inventory of `src/components`
  - Includes exports and direct in-repo consumers

Use these as the baseline map before deeper, feature-by-feature reviews.
